{
  "name": "@sosml/webworker",
  "version": "1.0.1",
  "description": "A webworker to run sosml-interpreter",
  "files": [
    "build/*.js",
    "build/*.d.ts"
  ],
  "main": "build/new_webworker.js",
  "scripts": {
    "build": "tsc",
    "version": "npm run build && git add -A",
    "postversion": "git push && git push --tags",
    "cilint": "tslint -c tslint.json 'src/**/*.ts'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SOSML/SOSML-webworker.git"
  },
  "author": "SOSML Team",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/node": "^7.0.22",
    "ts-loader": "^2.1.0",
    "tslint": "^5.3.2",
    "typescript": "2.3.4"
  },
  "dependencies": {
    "@sosml/interpreter": "^1.1.0"
  }
}
